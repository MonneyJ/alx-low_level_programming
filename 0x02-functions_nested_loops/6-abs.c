#include <stdlib.h>
/**
*_abs - absolute values
*@c: variable argument
*Description: a function that computes the absolute value of an integer
*Return: b
*/

int _abs(int c)
{
	int b = abs(c);

	return (b);
}
