#include <string.h>
/**
*_strlen - length of string
*@s: argument to be used
*Description: returns the lenght of a string
*Return: len
*/

int _strlen(char *s)
{
	int len;

	len = strlen(s);

	return (len);
}
